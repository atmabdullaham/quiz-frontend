1. Leaderboard not visible to attendi.
2. Admin can see leaderboard,
3. Admin can publish result from dashboard for each quizz,
4. It will show in different route, /quiz-result type link,
5. this is a different route which is in navbar button also like quizz,
6. when admin click on result publish icon for specific quiz,

- there will be two options, 1. classwise, 2. Overall,
  if select "classwise" then it will show a list with top 3 student from each class, and a delete button beside each, here, the admin only keep one student for each class and others will be deleted, after manually check admin can click on result publish button, then result will publish for this quiz, the quiz tittle is the result title, and publish the result, save the result in database and sync to result page, it will be dynamic result for each quiz result, and user can share this result link,

if select "overall" then show ovarall top 15 list of student who get top score, then admin can delete the student and make a final result with top 3 or 5 studnt, after manuall check the admin can click the button on publixh the reslut then result will save in database for this quiz and result will show as a card in results page, and also it will be adynamic result for quiz result and user can share this reslult via link,

and result route will be public page, anyone can see the result via link,
